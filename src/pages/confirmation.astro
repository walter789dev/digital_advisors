<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="shortcut icon" href="/logo.png" />
    <title>Confirmación de Formulario</title>
  </head>
  <style>
    body {
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      background-color: #f5f5f5;
    }

    .confirmation-container {
      padding: 2rem;
      text-align: center;
      max-width: 500px;
      width: 90%;
    }

    .success {
      color: #2e7d32;
      background-color: #e8f5e9;
      border-radius: 8px;
      padding: 1rem 2rem;
      margin-bottom: 1.5rem;
    }

    .error {
      color: #c62828;
      background-color: #ffebee;
      border-radius: 8px;
      padding: 1rem;
      margin-bottom: 1.5rem;
    }

    .icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    h1 {
      margin-top: 0;
      font-size: 1.5rem;
    }

    p {
      color: #555;
      line-height: 1.5;
    }

    .btnOk,
    .btnErr {
      background-color: #27405a;
      color: white;
      border: none;
      border-radius: 4px;
      padding: 0.8rem 1.5rem;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.3s;
      text-decoration: none;
      display: inline-block;
      margin-top: 1rem;
    }

    .none {
      display: none;
    }

    .btn:hover {
      background-color: #1565c0;
    }
  </style>

  <body>
    <div class="confirmation-container">
      <div class="success none">
        <div class="icon">✓</div>
        <h1>¡Enviado correctamente!</h1>
        <p>Tu formulario ha sido procesado con éxito. Gracias por tu envío.</p>
        <button class="btnOk"> Volver a inicio </button>
      </div>

      <div class="error none">
        <div class="icon">✗</div>
        <h1>Error en el envío</h1>
        <p>
          Lo sentimos, ha ocurrido un problema al procesar tu formulario. Por
          favor, inténtalo de nuevo.
        </p>
        <button class="btnErr"> Volver al formulario </button>
      </div>
    </div>
  </body>
</html>

<script>
  const btnOk = document.querySelector(".btnOk");
  const btnErr = document.querySelector(".btnErr");
  const error = document.querySelector(".error");
  const success = document.querySelector(".success");

  const queryString = window.location.search;
  const status = new URLSearchParams(queryString).get("status");

  if (status === "error") {
    error?.classList.remove("none");
  } else {
    success?.classList.remove("none");
  }

  btnOk?.addEventListener("click", () => (window.location.href = "/"));
  btnErr?.addEventListener("click", () => (window.location.href = "/contact"));
</script>
