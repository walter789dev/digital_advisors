---
import { Image } from "astro:assets"
import Button from "./Button.astro"
const {title, description, image} = Astro.props
---

<div class="relative flex items-center justify-between min-h-[250px] h-[35vh]">
    <div class="px-5 text-white text-center z-10">
        <h1 class="text-3xl font-semibold">{title}</h1>
        <p class="my-4">{description}</p>
        <Button text="Consulta gratis" />
    </div>
    <div class="size-full absolute inset-0 brightness-50 z-0">
        <Image class="size-full object-cover" src={image} alt="Imagen que acompaña al texto principal" />
    </div>
</div>